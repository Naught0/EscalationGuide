$(function() {
    $('.btn-icon').click(function() {
        // Eventually this is going to look kinda like this 
        // https://jsfiddle.net/pjQP7/10/
        // Slide content left or right or something
        if($(this).find($(".fa")).hasClass('fa')
        $("i.fa-toggle-on").toggleClass('fa-rotate-180');
    });
});